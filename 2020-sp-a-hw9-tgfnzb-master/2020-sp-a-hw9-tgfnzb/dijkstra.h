#pragma once
#include "MyGraph.h"


// Must be Dijkstra's with a priority queue.
// Takes as input a pointer to the graph,
// and the problem statement (as returned by the import_problem()
// Outputs the solution to be printed
// As always, don't edit the h file!
string dijkstra(MyGraph *, std::string);
